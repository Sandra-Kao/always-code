# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.3.0

      - name: Vue to Github Pages
        # You may pin to the exact commit or the version.
        # uses: xRealNeon/VuePagesAction@1fe4d0b38025bcbaafa71a359dd5c02f33bb03b7
        uses: xRealNeon/VuePagesAction@1.0.0
        with:
          # Your username
          username: Sandra.Kao
          # Your reponame
          reponame: always-code
          # Your Github token
          token: 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
          # Your git commit email
          gitemail: shuyukao0@gmail.com
          # Your git commit name
          gitname: Sandra-Kao
          # Your git commit message
          gitmsg: This is a gitmsg for deploy
          # Your custom domain
          cname: none

      - name: vue cli plugin s3 deploy action
        # You may pin to the exact commit or the version.
        # uses: jackdcasey/vue-cli-plugin-s3-deploy-action@f9697604b33e5ed7564ce511f30d0af81a2839a4
        uses: jackdcasey/vue-cli-plugin-s3-deploy-action@v0.1
